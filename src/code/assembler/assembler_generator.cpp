#include "../../headers/assembler/assembler_generator.hpp"

asmVec generateNumber(int64 num){
    asmVec result;
    return result;
}
asmVec generateConstants(){
    asmVec result;
    // for(auto& n : tree.getNumbers()){
    //     asmVec tmp = generateNumber(n);
    //     result.insert(result.end(), tmp.begin() , tmp.end());
    // }
    return result;
}